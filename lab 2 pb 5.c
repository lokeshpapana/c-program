#include<stdio.h>
int main(){
    float a,b;
    printf("the distance :");
    scanf("%f",&a);
    printf("the time taken :");
    scanf("%f",&b);
    printf("the speed is:%f",a/b);
    return 0;
}